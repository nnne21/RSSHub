{
  "version": 2,
  "framework": null,
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm build",
  "routes": [
    { "src": "/(.*)", "dest": "/api/vercel.ts" }
  ],
  "env": {
    "CACHE_TYPE": "memory",
    "ALLOW_USER_HOTLINK": "true"
  }
}
