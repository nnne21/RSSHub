{
  "version": 2,
  "builds": [
    {
      "src": "api/vercel.js",
      "use": "@vercel/node",
      "config": { "includeFiles": ["dist/**"] }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/vercel.js" }
  ],
  "buildCommand": "npm install --include=dev && npm run build && echo '--- ls at project root ---' && ls -la && echo '--- ls dist ---' && ls -la dist || (echo 'BUILD STEP FAILED'; exit 1)",
  "env": {
    "NODE_ENV": "development",
    "NPM_CONFIG_PRODUCTION": "false",
    "CACHE_TYPE": "memory",
    "ALLOW_USER_HOTLINK": "true"
  }
}
