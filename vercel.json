{
  "version": 2,
  "functions": {
    "api/**/*.js": { "runtime": "nodejs18.x" },
    "api/**/*.ts": { "runtime": "nodejs18.x" }
  },
  "env": {
    "CACHE_TYPE": "memory"
  },
  "installCommand": "yarn install --ignore-optional",
  "buildCommand": "yarn build || echo 'no build step'"
}
